name: Lint

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        name: Install NodeJS
        with:
          node-version: '24'

      - run: npm ci

      - run: npm run lint
